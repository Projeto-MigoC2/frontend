import 'package:flutter/material.dart';
import 'package:migoc2/assuntos/view_models/assuntos_view_model.dart';
import 'package:provider/provider.dart';

class AssuntosScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    AssuntosViewModel assuntosViewModel = context.watch<AssuntosViewModel>();
    return Scaffold(

    );
  }
}